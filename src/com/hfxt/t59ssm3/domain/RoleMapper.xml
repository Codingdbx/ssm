<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hfxt.t59ssm3.dao.RoleDao">

	<select id="getRoleById" resultType="role" parameterType="int">
		select * from tb_role where id=#{id}
	</select>
	
	<select id="getAllRoleName" resultType="role" >
	    select id,rolename from tb_role 
	</select>
	
	<!-- <resultMap type="role" id="rm_role">
	</resultMap> -->
</mapper>